# Развертывание на Render: Session Restoration

## 🚀 Подготовка к развертыванию

### 1. Проверить что все работает локально

```bash
# Запустить dev сервер
npm run dev

# Выполнить Тесты 1-5 из TESTING_SESSION_RESTORATION.md
# ✅ Все тесты должны пройти
```

### 2. Подготовить Render окружение

На Render нужно установить следующие переменные окружения:

```
VITE_PARSE_APP_ID      = XLiNP1abc123...
VITE_PARSE_JS_KEY      = abcdefg123456...
```

Опционально:
```
VITE_PARSE_REST_KEY    = tijJJ1234567...
```

---

## 📝 Шаг за шагом: Добавление на Render

### Шаг 1: Зайти в Render Dashboard

1. Откройте https://dashboard.render.com
2. Логинитесь с GitHub аккаунтом
3. Нажмите "New +" → "Web Service"

### Шаг 2: Выбрать репозиторий

1. Выберите ваш GitHub репозиторий (Gorizont)
2. Выберите branch (например, main)
3. Нажмите "Create Web Service"

### Шаг 3: Конфигурация базовых параметров

```
Name:                    gorizont-app
Environment:             Node
Region:                  Singapore (or closest to you)
Branch:                  main
Runtime:                 Node
Build Command:           npm install && npm run build
Start Command:           npm run preview
```

### Шаг 4: Добавить Environment Variables

Нажмите "Add Environment Variable" для каждой переменной:

#### 4.1 VITE_PARSE_APP_ID

```
Name:  VITE_PARSE_APP_ID
Value: XLiNP1abc123...  (ваш App ID)
```

#### 4.2 VITE_PARSE_JS_KEY

```
Name:  VITE_PARSE_JS_KEY
Value: abcdefg123456...  (ваш JS Key)
```

#### 4.3 VITE_PARSE_REST_KEY (опционально)

```
Name:  VITE_PARSE_REST_KEY
Value: tijJJ1234567...  (ваш REST Key)
```

### Шаг 5: Развертывание

1. Нажмите "Create Web Service"
2. Render начнет сборку (может занять 2-5 минут)
3. Дождитесь сообщения "Live" в статусе
4. Откройте URL приложения

---

## ✅ Проверка после развертывания

### 1. Открыть приложение на Render

```
https://your-app-name.onrender.com
```

### 2. Проверить консоль браузера (F12)

Должны быть логи:
```
Parse инициализирован
✅ Parse session restored from sessionToken
```

### 3. Выполнить базовый тест

```
1. Авторизоваться в приложении
2. Нажать F5
3. Проверить что пользователь остается залогинен
4. Выполнить операцию требующую auth (комментарий, файл)
```

### 4. Проверить Network tab

```
1. DevTools (F12) → Network
2. Выполнить операцию с auth
3. Найти запрос к Back4App
4. Проверить заголовок: X-Parse-Session-Token присутствует
```

---

## 🔧 Troubleshooting на Render

### ❌ Проблема: Build fails

**Логи ошибки:**
```
npm ERR! code EEXIT
npm ERR! errno 1
```

**Решение:**

1. Проверить что в репозитории есть `package.json`
2. Проверить что нет синтаксических ошибок в коде
3. Запустить локально: `npm run build`
4. Посмотреть логи на Render (нажать "View Build Logs")

### ❌ Проблема: "Parse не инициализирован" на Render

**Причина:** Переменные окружения не установлены

**Решение:**

1. Нажмите на service в Render Dashboard
2. Перейдите в "Environment"
3. Проверьте что VITE_PARSE_APP_ID и VITE_PARSE_JS_KEY установлены
4. Если пусто - добавьте их
5. Нажмите "Redeploy"

### ❌ Проблема: Пользователь разлогинен на Render

**Причина:** Возможно sessionToken строка или другая проблема

**Решение:**

1. Откройте DevTools на Render версии
2. В консоли выполните:
   ```javascript
   localStorage.getItem('market_user')
   Parse.User.current()
   ```
3. Если Parse.User.current() === null:
   - Проверьте VITE_PARSE_APP_ID и VITE_PARSE_JS_KEY в Render Environment
   - Нажмите F5 в браузере
4. Посмотрите логи Render:
   - Нажмите на service → "Logs"
   - Ищите ошибки при инициализации Parse

### ❌ Проблема: "Too many redirects" или CORS error

**Причина:** Неправильная конфигурация окружения

**Решение:**

1. Проверьте что VITE_PARSE_APP_ID правильный:
   ```javascript
   // В консоли:
   console.log(typeof (window as any).__VITE_PARSE_APP_ID__)
   // Должно быть: string
   ```

2. Проверьте что нет опечаток в ключах
3. Убедитесь что ключи из Back4App, а не из другого сервиса

---

## 📊 Мониторинг на Render

### Проверка логов

```
1. Откройте Render Dashboard
2. Нажмите на ваш service
3. Нажмите "Logs"
4. Ищите логи приложения (должны быть видны console.log)
```

### Проверка здоровья приложения

```
1. Нажмите на service
2. Проверьте статус: должно быть "Live" (зеленый)
3. Если "Crashed" (красный):
   - Нажмите "View Build Logs"
   - Посмотрите на последние ошибки
   - Нажмите "Redeploy" для пересборки
```

---

## 🔄 Автоматическое развертывание (CI/CD)

Render автоматически разворачивает при каждом commit в main branch.

### Отключить автоматическое развертывание (опционально)

1. Перейдите в Render Dashboard → Service Settings
2. Нажмите "Deploy Hook"
3. Скопируйте webhook URL
4. В GitHub:
   - Репозиторий → Settings → Webhooks
   - Отредактируйте webhook и удалите или отключите

### Ручное развертывание

```
1. Нажмите на service в Render Dashboard
2. Нажмите "Manual Deploy"
3. Выберите branch
4. Нажмите "Deploy latest commit"
```

---

## 📈 Performance на Render

### Ожидаемое время загрузки

- **Первая загрузка:** 2-5 секунд (при cold start)
- **Последующие загрузки:** <1 сек
- **После неиспользования:** Возможна задержка из-за Render's sleep

### Оптимизация

1. **Upgrade to Paid Plan:** Убрать sleep режим
2. **Use CDN:** Кешировать статические файлы
3. **Monitor Performance:**
   - DevTools → Performance tab
   - Ищите bottlenecks

---

## 🔐 Security на Render

### Переменные окружения

- ✅ VITE_PARSE_APP_ID - безопасна (published)
- ✅ VITE_PARSE_JS_KEY - безопасна (published)
- ⚠️ VITE_PARSE_REST_KEY - потенциально чувствительна (не используется на клиенте)

### Best Practices

1. **Не коммитьте .env файлы** - Render автоматически использует Environment Variables
2. **Используйте разные ключи** для dev и prod
3. **Регулярно меняйте ключи** если есть подозрение утечки
4. **Используйте HTTPS** - Render предоставляет автоматически

---

## 🧪 Testing на Render

Перед развертыванием убедитесь что:

- [ ] Все Тесты 1-5 из `TESTING_SESSION_RESTORATION.md` пройдены локально
- [ ] `npm run build` выполняется без ошибок
- [ ] Нет красных ошибок в консоли браузера
- [ ] sessionToken восстанавливается после F5
- [ ] Все операции требующие auth работают

---

## 📋 Checklist перед продакшеном

- [ ] Проверить переменные окружения на Render
- [ ] Запустить сборку локально: `npm run build`
- [ ] Протестировать все функции на dev сервере
- [ ] Развернуть на Render
- [ ] Протестировать на Render версии
- [ ] Проверить логи на Render (нет красных ошибок)
- [ ] Проверить производительность
- [ ] Убедиться что sessionToken восстанавливается
- [ ] Проверить Mobile версию
- [ ] Готово к использованию! ✅

---

## 📞 Получение помощи

### Если что-то не работает на Render:

1. **Проверить логи:**
   - Service → Logs
   - Ищите красные ошибки

2. **Проверить браузер:**
   - DevTools F12
   - Смотрите на Console и Network tabs

3. **Перестроить:**
   - Service → Manual Deploy
   - Нажмите "Deploy latest commit"

4. **Очистить кеш:**
   - Shift+Ctrl+Del в браузере
   - Clear All History
   - Нажмите F5

5. **Редактировать переменные:**
   - Service → Environment
   - Проверьте переменные
   - Нажмите "Redeploy"

---

## 🎉 После успешного развертывания

Приложение готово к использованию в продакшене!

- ✅ Пользователи остаются залогинены после F5
- ✅ SessionToken автоматически восстанавливается
- ✅ Все операции работают без повторной авторизации
- ✅ Seamless experience для пользователей

**Поздравляем! 🎊**
