# 🧪 Автоматический тест сессии и комментариев

## 🚀 БЫСТРЫЙ СТАРТ

### Открыте тестовый интерфейс:
```
http://localhost:3000/TEST_AUTO.html
```

---

## 📋 Пошаговые инструкции

### **Шаг 1️⃣: Откройте приложение**
1. На тестовом интерфейсе нажмите кнопку **"📱 Открыть приложение"**
2. Это откроет приложение в новой вкладке на `http://localhost:3000`
3. Новая вкладка должна загрузиться автоматически

### **Шаг 2️⃣: Войдите в систему**
В приложении (новой вкладке):
1. Перейдите на `/login` (или найдите форму входа)
2. Введите учётные данные:
   - **Логин:** `Vladikabh23`
   - **Пароль:** `111111`
3. Нажмите кнопку **"Войти"**
4. Дождитесь сообщения "Вход успешен!" и перенаправления

Затем вернитесь на тестовый интерфейс и нажмите:
- **"✓ Проверить вход"** (автоматическая проверка)
- Или **"👤 Я вошёл"** (если у вас уже проблемы)

### **Шаг 3️⃣: Добавьте комментарий**
В приложении:
1. Перейдите на **Каталог** (`/catalog`)
2. Выберите **любой товар** и откройте его (ProductDetails)
3. Прокрутите вниз до раздела **"Отзывы"** или **"Комментарии"**
4. Напишите комментарий, например:
   ```
   Отличный товар! Тестирую восстановление сессии.
   ```
5. Установите рейтинг **5 звёзд**
6. Нажмите кнопку **"Отправить"** или **"Отправить отзыв"**
7. Проверьте, что комментарий появился в списке выше

Затем вернитесь на тестовый интерфейс и нажмите:
- **"✓ Проверить комментарий"** (автоматическая проверка)
- Или **"📝 Добавить тестовый"** (если хотите автоматически добавить тестовый)

### **Шаг 4️⃣: Перезагрузите страницу (F5)**
В приложении (в первой вкладке с приложением):
1. Нажмите **F5** или **Ctrl+R** (Refresh)
2. Страница перезагрузится
3. Дождитесь полной загрузки

Затем вернитесь на тестовый интерфейс и нажмите:
- **"✓ Проверить после F5"** (автоматическая проверка результатов)

---

## ✅ Ожидаемые результаты

### Если всё работает правильно ✅
- ✅ **Комментарий остался** после F5
- ✅ **localStorage `market_reviews`** содержит ваш комментарий
- ✅ **Пользователь** остаётся залогинен (или разлогинен с 403 — это ОК)
- ✅ **Консоль** показывает логи Parse успешной инициализации

### Возможные сценарии

| Сценарий | Статус | Описание |
|----------|--------|---------|
| Комментарий остался, залогинен | ✅ ОТЛИЧНО | Всё работает идеально |
| Комментарий остался, разлогинен (403) | ✅ ОК | Токен истёк на сервере, но комментарии сохранены локально |
| Комментарий исчез | ❌ ОШИБКА | localStorage не сохраняется правильно |
| localStorage пуст | ❌ ОШИБКА | Проблема с сохранением в localStorage |
| Консоль: "Parse not initialized" | ❌ ОШИБКА | Parse SDK не инициализирован |

---

## 🔍 Отладка (если что-то не работает)

### Откройте DevTools браузера (F12) в приложении и проверьте:

#### **1. Консоль (Console)**
```javascript
// Скопируйте и выполните:
localStorage.getItem('market_reviews')
localStorage.getItem('market_user')
```

Если значения `null` → **проблема с localStorage**

#### **2. Application → Local Storage**
- Найдите ключ `market_reviews` → должен содержать JSON массив комментариев
- Найдите ключ `market_user` → должен содержать sessionToken

#### **3. Network → отследите запросы Parse**
- Поиск запросов к `parseapi.back4app.com`
- Статус 200 = хорошо, 403 = токен истёк (ОК), 4xx/5xx = ошибка

#### **4. Console логи**
Ищите в консоли логи вроде:
```
✅ Parse инициализирован успешно
✅ Parse session restored from sessionToken
// ИЛИ
Failed to restore Parse session (kept local saved user for UI fallback): ParseError: unauthorized
```

---

## 💡 Полезные команды консоли

Если хотите вручную проверить состояние, выполните в консоли браузера (F12):

```javascript
// Проверить все ключи localStorage
Object.keys(localStorage)

// Получить комментарии
const reviews = JSON.parse(localStorage.getItem('market_reviews') || '[]');
console.log('Комментариев:', reviews.length);
reviews.forEach(r => console.log(`- "${r.comment}" (rating: ${r.rating})`));

// Получить пользователя
const user = JSON.parse(localStorage.getItem('market_user') || '{}');
console.log('Пользователь:', user.username);
console.log('sessionToken:', user.sessionToken ? 'ЕСТЬ' : 'ОТСУТСТВУЕТ');

// Проверить Parse
const parseUser = Parse.User.current();
console.log('Parse.User.current():', parseUser ? 'ЗАЛОГИНЕН' : 'НЕ ЗАЛОГИНЕН');
```

---

## 🎯 Результаты теста

После выполнения всех шагов на тестовом интерфейсе появится:

### **📊 Подробный отчёт** с:
- ✅/❌ Статусы каждого шага
- 📈 Статистика (было комментариев → осталось комментариев)
- 🔗 Ссылки на исходные данные

---

## ⏱️ Примерное время

- Шаг 1-2 (открыть + логин): **5 минут**
- Шаг 3 (добавить комментарий): **2 минуты**
- Шаг 4-5 (F5 + проверка): **1 минута**

**Всего: ~8-10 минут**

---

## 🚨 Если возникают проблемы

### Проблема: Вкладка приложения не открывается
- ✅ Проверьте блокировщик popup в браузере
- ✅ Убедитесь что `localhost:3000` доступен
- ✅ Нажмите "📱 Открыть приложение" снова

### Проблема: Ошибка логина (Invalid username/password)
- ✅ Проверьте орфографию: `Vladikabh23` (заглавная V)
- ✅ Проверьте пароль: `111111` (шесть единиц)
- ✅ Убедитесь что пользователь существует на Back4App

### Проблема: Комментарий не добавляется
- ✅ Убедитесь что вы залогинены (корзина видна в меню)
- ✅ Попробуйте открыть консоль (F12) и проверить ошибки
- ✅ Нажмите "📝 Добавить тестовый" для автоматического добавления

### Проблема: localStorage пуст после F5
- ✅ Это может быть нормально если браузер удалил данные
- ✅ Проверьте что приватный режим НЕ включен
- ✅ Попробуйте очистить кэш браузера и повторить тест

---

## 📝 Логирование

Все действия тестового интерфейса логируются в **правой панели**.

Каждый лог имеет:
- ⏰ Временную метку
- 📊 Иконку статуса (✅ ✗ ⚠️ ℹ️)
- 📄 Описание действия

**Сохраните скриншот логов если возникнут проблемы!**

---

## 🎉 Успешное завершение теста

Когда все шаги выполнены успешно, вы увидите:

```
📊 Результаты теста
✅ Приложение открыто
✅ Пользователь залогинен
✅ Комментарии добавлены
✅ Комментарии сохранены после F5
✅ localStorage.market_reviews не потерян

Статистика:
• До F5: N комментариев
• После F5: N комментариев
• Потеряно: 0 комментариев ✅
```

---

## 📞 Если нужна помощь

Сохраните:
1. 📸 Скриншоты тестового интерфейса (логи)
2. 📸 Скриншоты консоли браузера (F12 → Console)
3. 📸 DevTools → Application → Local Storage (content)
4. 📝 Описание что пошло не так

И поделитесь этой информацией для диагностики! 🔍
