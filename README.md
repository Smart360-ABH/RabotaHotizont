# Экосистема Market 🌍

Современный маркетплейс книг и канцелярских товаров с использованием React, TypeScript, Tailwind CSS и искусственного интеллекта Google Gemini.

## ✨ Особенности

*   **Интерактивный UI/UX:**
    *   Эффектная вступительная анимация ("шторка") с 3D визуализацией Земли и спутников.
    *   3D Hero-секция с интерактивной картой лояльности (Platinum Card).
    *   Кнопки категорий в стиле механической клавиатуры с подсветкой.
    *   Стеклянный морфизм (Glassmorphism) на карточках товаров.
*   **Умная Темизация:**
    *   Автоматическое переключение на ночной режим после 21:00.
    *   Плавная анимация смены дня и ночи (градиенты, звезды).
    *   Ручной переключатель темы.
*   **AI Возможности (Google Gemini):**
    *   **Умный помощник:** Чат-бот для помощи в выборе товаров.
    *   **Генерация контента:** Автоматическое создание описаний товаров в админ-панели.
*   **Функционал магазина:**
    *   Каталог с фильтрацией и сортировкой.
    *   Корзина с динамическим расчетом стоимости.
    *   Избранное.
    *   Админ-панель с графиками продаж.

## 🛠 Технологический стек

*   **Frontend:** React 19, TypeScript
*   **Стилизация:** Tailwind CSS, Custom CSS Animations
*   **Маршрутизация:** React Router v7
*   **Иконки:** Lucide React
*   **Графики:** Recharts
*   **AI:** Google GenAI SDK

---

## 🚀 Запуск локально

Для запуска проекта на вашем компьютере вам понадобится [Node.js](https://nodejs.org/).

1.  **Клонируйте репозиторий (если есть) или скачайте файлы:**
    ```bash
    git clone https://github.com/your-username/ecosystem-market.git
    cd ecosystem-market
    ```

2.  **Установите зависимости:**
    ```bash
    npm install
    ```

3.  **Настройка переменных окружения:**
    Создайте файл `.env` в корне проекта и добавьте ваш API ключ от Google Gemini:
    ```env
    REACT_APP_API_KEY=ваш_ключ_здесь
    # Или для Vite:
    VITE_API_KEY=ваш_ключ_здесь
    ```

4.  **Запустите проект:**
    ```bash
    npm run dev
    # или
    npm start
    ```

---

## 📦 Пуш в GitHub (Git)

Если вы хотите сохранить код в репозитории GitHub:

1.  **Инициализация Git:**
    ```bash
    git init
    ```

2.  **Добавление файлов:**
    ```bash
    git add .
    ```

3.  **Первый коммит:**
    ```bash
    git commit -m "Initial commit: Ecosystem Market finish"
    ```

4.  **Связь с GitHub:**
    *   Создайте новый пустой репозиторий на [GitHub](https://github.com/new).
    *   Выполните команды, предложенные GitHub:
    ```bash
    git branch -M main
    git remote add origin https://github.com/ВАШ_ЛОГИН/НАЗВАНИЕ_РЕПОЗИТОРИЯ.git
    git push -u origin main
    ```

---

## ☁️ Деплой (Публикация в интернет)

Лучший способ опубликовать React-приложение бесплатно — использовать **Vercel** или **Netlify**.

### Вариант 1: Vercel (Рекомендуемый)

1.  Зарегистрируйтесь на [Vercel.com](https://vercel.com).
2.  Нажмите **"Add New..."** -> **"Project"**.
3.  Импортируйте ваш Git-репозиторий.
4.  **Важно:** В разделе **Environment Variables** добавьте ваш API ключ:
    *   Key: `API_KEY` (или то имя, которое вы использовали в коде, например `VITE_API_KEY`)
    *   Value: `Ваш_Google_Gemini_Key`
5.  Нажмите **Deploy**.

### Вариант 2: Netlify

1.  Зарегистрируйтесь на [Netlify.com](https://www.netlify.com/).
2.  Нажмите **"Add new site"** -> **"Import from existing project"**.
3.  Выберите GitHub и ваш репозиторий.
4.  В настройках деплоя (Advanced build settings) добавьте переменную окружения (Environment Variable) с вашим ключом API.
5.  Нажмите **Deploy site**.

---

## 📂 Структура проекта

```
src/
├── components/      # Переиспользуемые компоненты (Карточки, Хедер, Анимации)
├── context/         # Управление состоянием (Корзина, Тема, Юзер)
├── pages/           # Страницы приложения (Главная, Каталог, Админка)
├── services/        # API запросы к Gemini
├── types.ts         # TypeScript интерфейсы
├── constants.ts     # Моковые данные товаров
└── App.tsx          # Корневой компонент
```

---

**Разработчик:** Экосистема Team
**Контакты:** service-abh@yandex.ru

---

## 🔌 Back4App / Parse

Интеграция с Back4App (Parse) используется для хранения и CRUD-операций. Ниже краткая инструкция по подключению.

1) Установка клиента Parse:

```powershell
npm install parse --save
```

2) Добавьте переменные окружения (рекомендуется Vite-переменные): создайте `.env.local` или используйте шаблон `.env.example` и положите реальные значения в `.env` (НЕ коммитьте реальные ключи):

```
VITE_PARSE_APP_ID=ВАШ_APP_ID
VITE_PARSE_JS_KEY=ВАШ_JS_KEY
```

3) В проекте уже есть helper в `services/back4app.ts`. Инициализация выполняется автоматически при старте приложения (в `App.tsx` вызывается `initializeParse()`), если переменные заданы.

4) Примеры CRUD (см. `services/back4app.ts`):

- Создать:
```ts
import { createGameScore } from './services/back4app';
await createGameScore(1337, 'Sean Plott');
```

- Прочитать:
```ts
import { queryGameScores } from './services/back4app';
const results = await queryGameScores(1000);
```

- Обновить:
```ts
import { updateGameScore } from './services/back4app';
await updateGameScore('OBJECT_ID', { score: 1338 });
```

- Удалить:
```ts
import { deleteGameScore } from './services/back4app';
await deleteGameScore('OBJECT_ID');
```

Совет по безопасности: не кладите реальные ключи в репозиторий; используйте `.env.local` и добавьте его в `.gitignore`.
