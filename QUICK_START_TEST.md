# 🚀 Быстрый старт: Тестирование восстановления сессии

## 5-минутный тест

### Шаг 1: Подготовка (1 минута)

```bash
# 1. Убедиться что dev сервер запущен
npm run dev

# 2. Открыть браузер
# http://localhost:5173

# 3. Открыть DevTools: F12
# Выберите вкладку "Console"
```

### Шаг 2: Авторизация (2 минуты)

```
1. Нажмите на логин/профиль в приложении
2. Введите учетные данные (username/password)
3. Нажмите Login

Ожидаемый результат в консоли:
┌──────────────────────────────────────┐
│ Parse инициализирован                │
│ ✅ Parse session restored from       │
│    sessionToken                      │
│ ✅ User session restored successfully│
└──────────────────────────────────────┘
```

### Шаг 3: Проверка сессии (1 минута)

Выполните в консоли (F12 → Console):

```javascript
// Проверка 1: Пользователь в React контексте
localStorage.getItem('market_user')
// Должен вернуть JSON с sessionToken

// Проверка 2: Пользователь в Parse SDK
Parse.User.current()
// Должен вернуть объект пользователя (не null)

// Проверка 3: SessionToken доступен
Parse.User.current().getSessionToken()
// Должен вернуть строку типа "r:xyz..."
```

### Шаг 4: Тестирование восстановления (1 минута)

```
1. Нажмите F5 (или Ctrl+R)
   
2. В консоли должны появиться те же логи:
   ✅ Parse инициализирован
   ✅ Parse session restored from sessionToken
   ✅ User session restored successfully

3. Проверьте: Пользователь остается залогинен
   └─ Видно в профиле / заголовке
   └─ Нет редиректа на /login
```

**✅ УСПЕХ:** Если пользователь остается залогинен после F5!

---

## Если что-то не работает

### ❌ Проблема: "Parse не инициализирован"

**Решение:**
```bash
# 1. Проверьте .env.local
cat .env.local

# Должно быть:
# VITE_PARSE_APP_ID=...
# VITE_PARSE_JS_KEY=...

# 2. Если переменных нет - добавьте их

# 3. Перезапустите dev сервер
# Ctrl+C в терминале, затем:
npm run dev
```

### ❌ Проблема: Пользователь разлогинен после F5

**Проверьте в консоли:**
```javascript
// 1. Есть ли пользователь в localStorage?
localStorage.getItem('market_user')
// Если пусто → нужно залогиниться заново

// 2. Есть ли sessionToken?
const user = JSON.parse(localStorage.getItem('market_user'))
user.sessionToken // Должна быть строка, не undefined

// 3. Может ли Parse получить пользователя?
Parse.User.current() // Должен быть не null
```

### ❌ Проблема: Ошибка в консоли при восстановлении

**Проверьте:**
```javascript
// Какая ошибка выводится?
// Скопируйте текст ошибки и посмотрите в:
// - SESSION_RESTORATION_FIX.md
// - TESTING_SESSION_RESTORATION.md
```

---

## Интерпретация логов

### ✅ Успешное восстановление

```
Parse инициализирован
✅ Parse session restored from sessionToken
✅ User session restored successfully
```

**Что это означает:**
- Parse SDK инициализирован ✅
- sessionToken найден в localStorage ✅
- Back4App валидировал token ✅
- Пользователь восстановлен ✅

### ⚠️ Отсутствие сессии для восстановления

```
Parse инициализирован
ℹ️ No valid session to restore
```

**Что это означает:**
- Parse SDK инициализирован ✅
- localStorage пуст или нет sessionToken
- Пользователь не авторизован
- Нужно залогиниться

### ❌ Ошибка при восстановлении

```
Parse инициализирован
Failed to restore Parse session: [object Object]
```

**Что это означает:**
- Parse SDK инициализирован ✅
- sessionToken найден в localStorage
- ❌ Back4App отклонил token (истек, невалиден, или ошибка сервера)
- Пользователь разлогинен, localStorage очищен

---

## Визуальный контрольный список

### При открытии приложения

- [ ] В консоли видны логи "Parse инициализирован"
- [ ] Нет красных ошибок в консоли
- [ ] Пользователь видно в интерфейсе (профиль, имя)

### При нажатии F5

- [ ] Страница перезагружается полностью
- [ ] В консоли появляются логи инициализации
- [ ] Пользователь остается в том же месте
- [ ] Пользователь остается залогинен
- [ ] Нет редиректа на /login

### При выполнении операций требующих auth

- [ ] Комментарий добавляется сразу
- [ ] Файл загружается без ошибок
- [ ] Избранное добавляется сразу
- [ ] Нет запросов на авторизацию
- [ ] Нет ошибок 403/401 в Network tab (F12)

---

## Quick Debug Commands

Полезные команды для быстрой отладки:

```javascript
// Показать текущего пользователя
console.table(Parse.User.current())

// Показать sessionToken
console.log(Parse.User.current()?.getSessionToken())

// Показать сохраненного пользователя
console.table(JSON.parse(localStorage.getItem('market_user')))

// Проверить статус инициализации
console.log({
  parseInitialized: typeof Parse !== 'undefined',
  parseServerURL: Parse?.serverURL,
  userStored: !!localStorage.getItem('market_user'),
  userSession: !!Parse.User.current(),
})

// Вручную очистить сессию (для тестирования)
localStorage.removeItem('market_user')
// Потом F5 → пользователь должен быть разлогинен
```

---

## Проверка в Network Tab (F12)

Когда выполняется операция требующая auth (например, добавление комментария):

1. Откройте DevTools: F12
2. Перейдите на вкладку "Network"
3. Выполните операцию
4. Найдите запрос к Back4App (обычно POST)
5. Нажмите на запрос → "Headers" tab
6. Найдите заголовок: `X-Parse-Session-Token: r:...`

**✅ Если заголовок присутствует:** sessionToken успешно передается!
**❌ Если заголовка нет:** Проблема с получением sessionToken

---

## Быстрые тесты

### Тест 1: Базовое восстановление (30 сек)
```
1. Login
2. Проверить: Parse.User.current() ≠ null
3. F5
4. Проверить: Parse.User.current() ≠ null
```
✅ Готово!

### Тест 2: Несколько F5 подряд (1 мин)
```
1. Login
2. F5, F5, F5, F5, F5
3. Каждый раз проверить логи в консоли
4. Пользователь должен остаться залогинен
```
✅ Готово!

### Тест 3: Операция требующая auth (1 мин)
```
1. Login
2. F5
3. Добавить комментарий ИЛИ загрузить файл
4. Должно сработать без запроса на авторизацию
```
✅ Готово!

---

## Таблица решения проблем

| Проблема | Диагноз | Решение |
|----------|---------|--------|
| Пользователь разлогинен после F5 | sessionToken в localStorage но Parse SDK не инициализирован | Убедитесь что `.env.local` содержит VITE_PARSE_APP_ID и VITE_PARSE_JS_KEY |
| Ошибка "Invalid session token" | sessionToken истек на Back4App | Залогиниться заново (это норма, sessionToken имеют TTL) |
| Ошибка "Parse не инициализирован" | APP_ID или JS_KEY не установлены | Добавьте переменные в `.env.local` и перезапустите dev server |
| Комментарий требует повторного логина | sessionToken не передается в запрос | Проверьте что Parse.User.current() ≠ null в консоли |
| Логи не видны в консоли | Консоль закрыта или очищена | Откройте DevTools (F12), перейдите на Console tab, нажмите F5 |

---

## 🎉 После успешного тестирования

Если все 5 минут работали без проблем:

1. **Пройдите полный тест:** `TESTING_SESSION_RESTORATION.md`
2. **Запустите на Render:** Убедитесь что `.env` содержит необходимые переменные
3. **Используйте в продакшене:** Пользователи не будут терять сессии при обновлении!

---

**Готово к тестированию? Начните с Шага 1! ↑**
