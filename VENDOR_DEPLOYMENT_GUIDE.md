# 🚀 Развертывание Личного кабинета Вендора

Полное руководство по развертыванию и запуску Личного кабинета на production.

---

## 📋 Предварительная подготовка

### Список файлов для проверки

```
✅ src/services/parseSDK.ts                  (400+ строк)
✅ src/components/VendorDashboard.tsx        (300+ строк)
✅ src/components/VendorProducts.tsx         (400+ строк)
✅ src/components/VendorOrders.tsx           (350+ строк)
✅ src/components/VendorFinance.tsx          (400+ строк)
✅ src/components/VendorSettings.tsx         (450+ строк)
✅ src/components/VendorAuth.tsx             (350+ строк)
✅ VENDOR_DASHBOARD_GUIDE.md                 (Документация)
✅ VENDOR_CODE_EXAMPLES.md                   (Примеры)
✅ VENDOR_INTEGRATION_CHECKLIST.md           (Чек-лист)
✅ README_VENDOR_DASHBOARD.md                (Краткое описание)
```

---

## 🔧 Локальная разработка

### 1. Установка зависимостей

```bash
# Установка npm пакетов
npm install

# Если используете yarn
yarn install

# Если используете pnpm
pnpm install
```

Убедитесь, что установлены:
- `parse` ^7.0.2
- `recharts` ^3.5.0
- `react` ^19.2.0
- `react-router-dom` ^7.9.6
- `lucide-react` ^0.555.0

### 2. Создание .env.local

Скопируйте ваши ключи с Back4App:

```env
# Back4App API Keys
VITE_PARSE_APP_ID=your_app_id_here
VITE_PARSE_JS_KEY=your_js_key_here
VITE_PARSE_REST_KEY=your_rest_key_here

# Optional: для облачных функций
VITE_PARSE_SERVER_URL=https://parseapi.back4app.com
```

Как получить ключи:
1. Перейдите на [back4app.com](https://www.back4app.com)
2. Выберите ваше приложение
3. App Settings → Security Keys
4. Скопируйте Application ID, JavaScript Key, REST API Key

### 3. Инициализация Parse

Убедитесь, что в `main.tsx` вызывается:

```typescript
import * as parseSDK from './services/parseSDK'

// Инициализируем Parse
parseSDK.initializeParse()
```

### 4. Запуск разработки

```bash
npm run dev
```

Приложение будет доступно на `http://localhost:5173`

### 5. Тестирование

Откройте консоль браузера и проверьте:

```javascript
// Проверяем инициализацию
console.log(parseSDK)

// Проверяем подключение
const user = parseSDK.getCurrentUser()
console.log('Текущий пользователь:', user)
```

---

## 🏗️ Build и производство

### 1. Создание production build

```bash
npm run build
```

Это создаст оптимизированную версию в папке `dist/`

### 2. Проверка build

```bash
# Локальный preview
npm run preview

# Проверяем размер
du -sh dist/
```

### 3. Оптимизация

Проверьте размер bundle:

```bash
npm run build -- --report
```

---

## ☁️ Развертывание на хостинг

### Вариант 1: Vercel (рекомендуется для Vite)

```bash
# Установка CLI
npm i -g vercel

# Логин в Vercel
vercel login

# Развертывание
vercel

# Production deploy
vercel --prod
```

**Предварительно** добавьте переменные окружения в Project Settings:
- VITE_PARSE_APP_ID
- VITE_PARSE_JS_KEY
- VITE_PARSE_REST_KEY

### Вариант 2: Netlify

```bash
# Установка CLI
npm i -g netlify-cli

# Логин
netlify login

# Deploy
netlify deploy --prod

# Или используйте Git integration
# Просто выберите репозиторий в панели Netlify
```

### Вариант 3: GitHub Pages

```bash
# Добавьте в vite.config.ts
export default {
  base: '/Gorizont/', // если репо на подпути
  // ...
}

# Build
npm run build

# Deploy (требует gh-pages пакет)
npm i --save-dev gh-pages
npx gh-pages -d dist
```

### Вариант 4: Собственный сервер (VPS/Dedicated)

```bash
# На сервере
ssh user@your-server.com

# Клонируем репозиторий
git clone https://github.com/your-repo/Gorizont.git
cd Gorizont

# Установка зависимостей
npm ci --production

# Build
npm run build

# Копируем dist на веб-сервер
# Для Nginx:
sudo cp -r dist/* /var/www/html/

# Для Apache:
sudo cp -r dist/* /var/www/

# Перезагружаем сервер
sudo systemctl restart nginx  # или apache2
```

---

## 🔐 Безопасность для production

### 1. Переменные окружения

```bash
# Никогда не коммитьте .env файлы
echo ".env.local" >> .gitignore

# На хостинге установите переменные в GUI
# Vercel: Project Settings → Environment Variables
# Netlify: Build & Deploy → Environment
```

### 2. Back4App безопасность

#### Правила ACL для Production:

**Class: Product**
```
Public Read: ✓ (можно просматривать)
Public Write: ✗ (нельзя создавать анонимно)
Require User: ✓
Find: ✓
Create: ✓ (только вендоры через CloudCode)
Update: Только владелец через CloudCode
Delete: Только владелец через CloudCode
```

**Class: Order**
```
Public Read: ✗
Public Write: ✗
Require User: ✓
Find: Только свои заказы
Create: Только авторизованные
Update: Вендор и покупатель
Delete: Никому
```

**Class: Transaction**
```
Public Read: ✗
Public Write: ✗
Require User: ✓
Find: Только свои
Create: Только через CloudCode
Update: Никому
Delete: Никому
```

#### Включение HTTPS

В Back4App это работает по умолчанию на `https://parseapi.back4app.com`

### 3. CORS (Cross-Origin Resource Sharing)

Убедитесь, что Back4App разрешает ваш домен:

Back4App Dashboard → App Settings → CORS Settings:
```
Allowed Origins: https://yoursite.com, https://www.yoursite.com
```

### 4. Rate Limiting (опционально)

Можно настроить на Back4App:
- Max requests per minute
- IP-based limiting
- User-based limiting

### 5. Мониторинг ошибок

Добавьте Sentry для отслеживания ошибок:

```bash
npm install @sentry/react @sentry/tracing
```

```typescript
// main.tsx
import * as Sentry from "@sentry/react"

Sentry.init({
  dsn: "https://your-key@sentry.io/your-project-id",
  environment: process.env.NODE_ENV,
})
```

---

## 📊 Мониторинг production

### 1. Логирование

Добавьте логирование операций:

```typescript
// analytics.ts
export function logEvent(event: string, data?: any) {
  if (process.env.NODE_ENV === 'production') {
    // Отправляем на сервис аналитики
    fetch('/api/analytics', {
      method: 'POST',
      body: JSON.stringify({ event, data, timestamp: new Date() })
    })
  }
}
```

### 2. Performance Monitoring

```typescript
// Используем Web Vitals
import { getCLS, getFID, getFCP, getLCP, getTTFB } from 'web-vitals'

getCLS(console.log)
getFID(console.log)
getFCP(console.log)
getLCP(console.log)
getTTFB(console.log)
```

### 3. Проверка статуса

```bash
# Проверяем доступность приложения
curl https://yoursite.com

# Проверяем API
curl https://parseapi.back4app.com/parse/ping
```

---

## 🚨 Решение проблем при развертывании

### Проблема: "Parse не инициализирован"
```typescript
// Решение: убедитесь, что parseSDK.initializeParse() вызван
// и переменные окружения установлены на хостинге
```

### Проблема: "CORS ошибка"
```
// Решение:
// 1. Back4App → App Settings → CORS Settings
// 2. Добавьте ваш домен в Allowed Origins
// 3. Очистите кэш браузера (Ctrl+Shift+Del)
```

### Проблема: "Изображения не загружаются"
```
// Решение:
// Используйте https (не http)
// Проверьте размер файла (< 100 MB)
// Убедитесь, что Parse.File работает
```

### Проблема: "Медленная загрузка"
```bash
# Решение:
# 1. Проверьте размер bundle
npm run build --report

# 2. Используйте Code Splitting
# 3. Оптимизируйте изображения
# 4. Включите gzip compression на хостинге
```

### Проблема: "Ошибки 502 Bad Gateway"
```
// Решение:
// 1. Проверьте статус Back4App
// 2. Проверьте лимиты запросов
// 3. Включите error boundary
```

---

## 📈 Масштабирование

### Когда нужно масштабировать?

- **200+ заказов в день** → Оптимизируйте queries
- **1000+ активных вендоров** → Используйте кэширование
- **10000+ товаров** → Добавьте индексы в БД
- **100k+ запросов в день** → Рассмотрите CDN

### Оптимизация запросов

```typescript
// Плохо: загружаем все товары
const products = await parseSDK.getProductsByVendor(vendorId)

// Хорошо: с пагинацией
const query = new Parse.Query('Product')
query.equalTo('vendorId', vendorId)
query.limit(20)
query.skip(0)
const products = await query.find()
```

### Кэширование на клиенте

```typescript
const cache = new Map()

export async function getCachedProducts(vendorId: string) {
  const cacheKey = `products_${vendorId}`
  
  if (cache.has(cacheKey)) {
    return cache.get(cacheKey)
  }
  
  const products = await parseSDK.getProductsByVendor(vendorId)
  cache.set(cacheKey, products)
  
  // Инвалидация кэша через 5 минут
  setTimeout(() => cache.delete(cacheKey), 5 * 60 * 1000)
  
  return products
}
```

---

## 📝 Чек-лист перед production

### Код и компоненты
- [ ] Все компоненты созданы и работают
- [ ] Нет console.error и console.warn
- [ ] TypeScript не имеет ошибок
- [ ] Все импорты правильные

### Функциональность
- [ ] Вход/выход работает
- [ ] CRUD операции работают
- [ ] Файлы загружаются корректно
- [ ] Графики отображаются
- [ ] Все формы валидируют данные

### Безопасность
- [ ] Env переменные установлены на хостинге
- [ ] Back4App ACL правила настроены
- [ ] CORS настроен
- [ ] Пароли не логируются

### Performance
- [ ] Bundle size приемлем (< 500KB gzipped)
- [ ] Изображения оптимизированы
- [ ] Лоадеры показываются
- [ ] Медленные операции асинхронны

### Мониторинг
- [ ] Логирование настроено
- [ ] Error tracking (Sentry) включен
- [ ] Analytics настроена
- [ ] Backup БД настроен

### Documentation
- [ ] README обновлен
- [ ] API документирована
- [ ] Инструкции по развертыванию понятны
- [ ] Контакты для поддержки указаны

---

## 🎬 Post-deployment

### 1. Проверка после развертывания

```bash
# Проверьте основные функции
1. Откройте /vendor/login
2. Попробуйте войти (или зарегистрироваться)
3. Проверьте загрузку товаров
4. Создайте новый товар
5. Проверьте финансовую аналитику
6. Посмотрите на мобильном
```

### 2. Информирование пользователей

```markdown
# Релиз: Новый Личный кабинет Вендора

Уважаемые продавцы!

Мы рады представить полностью обновленный Личный кабинет с новыми возможностями:

✅ Улучшенное управление товарами
✅ Быстрая обработка заказов
✅ Детальная финансовая аналитика
✅ Новый современный интерфейс

Ссылка: https://yoursite.com/vendor/dashboard

Если у вас есть вопросы, обратитесь к поддержке.
```

### 3. Сбор feedback

Добавьте форму обратной связи:

```typescript
export function FeedbackButton() {
  return (
    <button onClick={() => {
      const feedback = prompt('Ваше мнение:')
      if (feedback) {
        fetch('/api/feedback', {
          method: 'POST',
          body: JSON.stringify({ feedback, vendorId: user.id })
        })
      }
    }}>
      💬 Отправить отзыв
    </button>
  )
}
```

---

## 📞 Техническая поддержка

### Если что-то сломалось

1. **Проверьте консоль браузера** (F12 → Console)
2. **Проверьте статус Back4App** (https://status.back4app.com)
3. **Посмотрите логи** сервера (если доступны)
4. **Отключите расширения браузера** (могут блокировать)
5. **Очистите кэш** (Ctrl+Shift+Del)

### Коммуникация с Back4App

- Email: support@back4app.com
- Документация: https://www.back4app.com/docs
- Community Forum: https://community.back4app.com

---

## 🎉 Готово!

Ваш Личный кабинет Вендора развернут на production и готов к использованию!

**Дата развертывания**: 1 декабря 2025
**Версия**: 1.0
**Статус**: ✅ Активен

---

**Спасибо за использование! Успехов в развитии маркетплейса! 🚀**
