# ⚡ ЧТО НУЖНО СДЕЛАТЬ ВРУЧНУЮ - СПИСОК ДЕЙСТВИЙ

**Статус**: ⏳ **ОЖИДАЕТ ВАШЕГО ВЫПОЛНЕНИЯ**  
**Время на выполнение**: 1-7 часов  
**Критичность**: 🔴 **ОЧЕНЬ ВАЖНО**

---

## 🎯 ДЕЙСТВИЯ ПО ПРИОРИТЕТАМ

### 🔴 СРОЧНО - СЕГОДНЯ (30 минут)

#### ✅ ШАГ 1: Регенерировать API ключи (5 минут)

**ДЕЙСТВИЕ**: Откройте Back4App и создайте новые ключи

```
1. Откройте браузер
2. Перейдите: https://www.back4app.com
3. Войдите в аккаунт
4. Выберите ваше приложение
5. Settings → Security
6. Нажмите кнопку "Regenerate All Keys"
7. Скопируйте новые значения:
   - Application ID (новый)
   - JavaScript Key (новый)
   - REST API Key (новый)
```

**⚠️ ВАЖНО**: 
```
Старые ключи БОЛЬШЕ НЕ РАБОТАЮТ:
❌ XLiNP1wljZYgnjhgjy4RHrjvKx3OLKI6OCMwZQvA
❌ 9Aa5EmVv6ujuZhEfRcbvfCTZhSte721MaTXb5l7m
❌ BadiMhfYEd68wGyu2X4JslEQsDIGJCqsBpc0cOBn
```

---

#### ✅ ШАГ 2: Обновить .env.local (2 минуты)

**ФАЙЛ**: `c:\Users\Владислав\Documents\GitHub\Gorizont\.env.local`

**БЫЛО**:
```env
VITE_PARSE_APP_ID=XLiNP1wljZYgnjhgjy4RHrjvKx3OLKI6OCMwZQvA
VITE_PARSE_JS_KEY=9Aa5EmVv6ujuZhEfRcbvfCTZhSte721MaTXb5l7m
VITE_PARSE_REST_KEY=BadiMhfYEd68wGyu2X4JslEQsDIGJCqsBpc0cOBn
```

**НУЖНО ЗАМЕНИТЬ НА**:
```env
VITE_PARSE_APP_ID=NEW_APP_ID_ИЗ_BACK4APP
VITE_PARSE_JS_KEY=NEW_JS_KEY_ИЗ_BACK4APP
# VITE_PARSE_REST_KEY=НЕ ДОБАВЛЯЙТЕ! (только на сервере)
```

**КАК СДЕЛАТЬ**:
1. Откройте `.env.local` в редакторе (VS Code)
2. Замените значения на новые из Back4App
3. Сохраните файл (Ctrl+S)
4. Готово!

---

#### ✅ ШАГ 3: Протестировать приложение (10 минут)

**КОМАНДЫ в терминале**:

```bash
# 1. Перейдите в папку проекта
cd c:\Users\Владислав\Documents\GitHub\Gorizont

# 2. Установите зависимости
npm install

# 3. Запустите dev сервер
npm run dev

# 4. Откройте в браузере
# http://localhost:5173
```

**ПРОВЕРКА**:
- [ ] Страница загружается без ошибок
- [ ] DevTools (F12) → Console: нет красных ошибок
- [ ] Видна форма входа (VendorAuth)
- [ ] Нет упоминания REST_KEY в console

---

#### ✅ ШАГ 4: Коммитить изменения (3 минуты)

**КОМАНДЫ в PowerShell**:

```powershell
cd c:\Users\Владислав\Documents\GitHub\Gorizont

# 1. Проверить что изменилось
git status

# 2. Добавить файлы
git add vite.config.ts
git add SECURITY*.md
git add QUICK_SECURITY*.md
git add .env.example

# 3. Создать коммит
git commit -m "🔒 security: remove REST_KEY from client and add security audit"

# 4. Отправить в GitHub
git push origin main
```

**РЕЗУЛЬТАТ**: Изменения загружены на GitHub ✅

---

### 🟡 ВАЖНЫЕ - НА ЭТОЙ НЕДЕЛЕ (2-3 часа)

#### 🔧 ШАГ 5: Настроить ACL правила (15 минут)

**ДЕЙСТВИЕ**: Установить разрешения в Back4App для каждого класса

**ШАГ 5A - Для класса Product**:

```
1. Back4App Dashboard
2. Data Browser → Product
3. Нажать на 🔒 (Security)
4. Settings for class permissions:

   PUBLIC:
   ☐ Find Objects: ✅ ДА (чтобы видели товары)
   ☐ Get: ✅ ДА
   ☐ Create: ❌ НЕТ
   ☐ Update: ❌ НЕТ
   ☐ Delete: ❌ НЕТ

   AUTHENTICATED:
   ☐ Create: ✅ ДА (вендоры могут создавать)
   ☐ Update: ⚠️ ONLY FOR OWNER
   ☐ Delete: ⚠️ ONLY FOR OWNER

5. Нажать Save
```

**ШАГ 5B - Для класса Order**:

```
PUBLIC:
☐ All: ❌ НЕТ (скрыть)

AUTHENTICATED:
☐ Find: ✅ ДА (смотреть свои заказы)
☐ Create: ✅ ДА (создавать заказы)
☐ Update: ⚠️ ТОЛЬКО для OWNER

VENDOR:
☐ Update: ✅ ДА (обновлять статус)
```

**ШАГ 5C - Для класса User**:

```
PUBLIC:
☐ All: ❌ НЕТ

AUTHENTICATED:
☐ Get: ⚠️ ТОЛЬКО СЕБЯ
☐ Update: ⚠️ ТОЛЬКО СЕБЯ
```

**ПРОВЕРКА**: Откройте Back4App и убедитесь что ACL установлены ✅

---

#### 🔧 ШАГ 6: Создать Cloud Code функции (30 минут)

**ДЕЙСТВИЕ**: Добавить безопасные функции на Back4App

**ФУНКЦИЯ 1: updateProductImage** (для безопасной загрузки)

```
1. Back4App Dashboard
2. Cloud Code → Functions
3. Нажать + (Add Function)
4. Name: updateProductImage
5. Вставить код:
```

```javascript
Parse.Cloud.define('updateProductImage', async (request) => {
  const { productId, imageUrl } = request.params;
  const user = request.user;
  
  // Проверка авторизации
  if (!user) {
    throw new Parse.Error(401, 'Требуется авторизация');
  }
  
  // Проверка прав
  const Product = Parse.Object.extend('Product');
  const query = new Parse.Query(Product);
  const product = await query.get(productId);
  
  if (!product) {
    throw new Parse.Error(404, 'Товар не найден');
  }
  
  if (product.get('vendorId') !== user.id) {
    throw new Parse.Error(403, 'Вы не владелец этого товара');
  }
  
  // Обновление
  product.set('image', imageUrl);
  product.set('updatedAt', new Date());
  
  // Логирование
  console.log(`[AUDIT] User ${user.id} updated product ${productId}`);
  
  return product.save();
});
```

```
6. Нажать Save
```

**ФУНКЦИЯ 2: createOrder** (для безопасного создания заказов)

```
1. Add Function
2. Name: createOrder
3. Вставить код:
```

```javascript
Parse.Cloud.define('createOrder', async (request) => {
  const { items, address } = request.params;
  const user = request.user;
  
  if (!user) {
    throw new Parse.Error(401, 'Требуется авторизация');
  }
  
  if (!items || items.length === 0) {
    throw new Parse.Error(400, 'Заказ должен содержать товары');
  }
  
  // Валидация цен товаров
  let total = 0;
  for (const item of items) {
    const Product = Parse.Object.extend('Product');
    const query = new Parse.Query(Product);
    const product = await query.get(item.productId);
    
    if (!product) {
      throw new Parse.Error(404, `Товар ${item.productId} не найден`);
    }
    
    // Защита от манипуляции ценой
    if (product.get('price') !== item.price) {
      throw new Parse.Error(400, 'Цена товара изменилась, перезагрузите');
    }
    
    total += item.price * item.quantity;
  }
  
  // Создание заказа
  const Order = Parse.Object.extend('Order');
  const order = new Order();
  order.set('customerId', user.id);
  order.set('items', items);
  order.set('address', address);
  order.set('total', total);
  order.set('status', 'pending');
  
  // ACL: только владелец может видеть
  const acl = new Parse.ACL(user);
  acl.setPublicReadAccess(false);
  order.setACL(acl);
  
  console.log(`[AUDIT] User ${user.id} created order for ${total}`);
  
  return order.save();
});
```

```
4. Нажать Save
```

**ПРОВЕРКА**: Функции видны в Back4App Cloud Code ✅

---

#### 🔧 ШАГ 7: Обновить документацию (10 минут)

**ФАЙЛ**: `c:\Users\Владислав\Documents\GitHub\Gorizont\README.md`

**ДОБАВИТЬ в конец файла**:

```markdown

## 🔒 Безопасность

### Переменные окружения

1. Скопируйте `.env.example` в `.env.local`
2. Добавьте свои ключи из Back4App Settings → Keys
3. **НЕ коммитьте `.env.local`** (он в .gitignore ✓)

```env
VITE_PARSE_APP_ID=your_app_id
VITE_PARSE_JS_KEY=your_js_key
```

### REST_KEY - ТОЛЬКО НА СЕРВЕРЕ

REST_KEY используется ТОЛЬКО в:
- Cloud Code функциях
- Backend API
- **НИКОГДА на клиенте!**

### Best Practices

- ✓ Используйте `.env.local` для development
- ✓ Используйте переменные окружения хостера для production
- ✓ Регулярно ротируйте ключи в Back4App
- ✓ Логируйте все операции
- ✓ Используйте Cloud Code для чувствительных операций
- ✓ Настройте ACL правила для защиты данных

### Безопасность документация

Полная документация по безопасности в файлах:
- `SECURITY_AUDIT.md` - Анализ проблем
- `SECURITY_ARCHITECTURE.md` - Архитектура
- `SECURITY_CHECKLIST.md` - Чеклист
- `QUICK_SECURITY_FIX.md` - Быстрый старт
```

```
Сохранить файл (Ctrl+S)
```

---

### 🔴 ПЕРЕД PRODUCTION (2-3 часа)

#### ✅ ШАГ 8: Финальная проверка (30 минут)

**ПРОВЕРКА 1**: Нет REST_KEY в коде

```bash
cd c:\Users\Владислав\Documents\GitHub\Gorizont

# Проверить что REST_KEY нет в браузере
grep -r "REST_KEY" src/ --include="*.tsx" --include="*.ts"
# ✅ Не должно быть ничего (кроме .env и конфига)

# Проверить что нет в git истории
git log --all -p | grep "REST_KEY"
# ✅ Если есть - нужно очистить историю!
```

**ПРОВЕРКА 2**: .env.local в .gitignore

```bash
# Проверить что он там
cat .gitignore | grep "*.local"
# ✅ Должно быть: *.local

# Проверить что .env.local не будет закоммичен
git status | grep ".env"
# ✅ Не должно быть .env.local (должен быть красным)
```

**ПРОВЕРКА 3**: vite.config.ts исправлен

```bash
# Проверить что REST_KEY удален
cat vite.config.ts | grep "REST_KEY"
# ✅ Не должно быть ничего!
```

---

#### ✅ ШАГ 9: Переменные окружения на хостере (15 минут)

**ЕСЛИ VERCEL**:

```
1. Откройте: https://vercel.com
2. Project Settings
3. Environment Variables
4. Добавить:
   VITE_PARSE_APP_ID = (значение из Back4App)
   VITE_PARSE_JS_KEY = (значение из Back4App)
5. НЕ добавляйте REST_KEY!
6. Нажать Save
7. Redeploy проект
```

**ЕСЛИ NETLIFY**:

```
1. Откройте: https://app.netlify.com
2. Site settings → Build & deploy → Environment
3. Нажать Edit variables
4. Добавить переменные с префиксом VITE_
5. Нажать Save
6. Trigger redeploy
```

**ЕСЛИ VPS / СОБСТВЕННЫЙ СЕРВЕР**:

```bash
# SSH в сервер
ssh user@your-server.com

# Обновить .env.local
nano /path/to/project/.env.local
# Обновить значения, Ctrl+X, Y, Enter

# Установить правильные разрешения
chmod 600 /path/to/project/.env.local

# Перезапустить приложение
pm2 restart all
```

---

#### ✅ ШАГ 10: Финальное тестирование (30 минут)

**ТЕСТ 1**: Функциональность

```
1. Откройте production URL (или http://localhost:5173)
2. Попробуйте:
   [ ] Создать аккаунт вендора
   [ ] Залогиниться
   [ ] Создать товар
   [ ] Отредактировать товар
   [ ] Создать заказ
   [ ] Обновить статус заказа
```

**ТЕСТ 2**: Безопасность

```
1. Откройте DevTools (F12)
2. Network tab:
   [ ] REST_KEY НЕ видна в заголовках ✓
   [ ] sessionToken ЕС в заголовках ✓
3. Console tab:
   [ ] Нет ошибок безопасности ✓
   [ ] Нет логирования ключей ✓
4. Application tab:
   [ ] localStorage содержит только sessionToken ✓
```

**ТЕСТ 3**: Back4App логи

```
1. Back4App Dashboard
2. Logs
3. Проверить:
   [ ] Нет подозрительной активности
   [ ] Нет ошибок безопасности
   [ ] Все операции залогированы
```

---

## 📋 ИТОГОВЫЙ ЧЕКЛИСТ

### Сегодня (необходимо выполнить):
```
[ ] Регенерировать ключи в Back4App (5 мин)
[ ] Обновить .env.local (2 мин)
[ ] Протестировать npm run dev (10 мин)
[ ] Коммитить изменения (3 мин)
────────────────────────────────────────
ИТОГО: 20 минут
```

### На этой неделе:
```
[ ] Настроить ACL правила (15 мин)
[ ] Создать Cloud Code функции (30 мин)
[ ] Обновить README.md (10 мин)
────────────────────────────────────────
ИТОГО: 55 минут (2-3 часа с перерывами)
```

### Перед production:
```
[ ] Финальная проверка безопасности (30 мин)
[ ] Переменные окружения на хостере (15 мин)
[ ] Финальное тестирование (30 мин)
────────────────────────────────────────
ИТОГО: 75 минут (2-3 часа)
```

---

## 🎯 КРАТКАЯ ИНСТРУКЦИЯ

### За 30 минут до работающего приложения:

```bash
# 1. Регенерируйте ключи в Back4App
# (откройте браузер, Settings → Security → Regenerate)

# 2. Обновите .env.local
# (замените VITE_PARSE_APP_ID и VITE_PARSE_JS_KEY)

# 3. Запустите
cd c:\Users\Владислав\Documents\GitHub\Gorizont
npm install
npm run dev

# 4. Коммитьте
git add vite.config.ts SECURITY*.md .env.example
git commit -m "🔒 security: remove REST_KEY from client"
git push origin main
```

---

## 🆘 ЕСЛИ ЧТО-ТО НЕ РАБОТАЕТ

### Проблема: "Нет ошибок, но ничего не работает"
```
→ Проверьте что .env.local содержит НОВЫЕ ключи из Back4App
→ Перезагрузите npm run dev (Ctrl+C и заново)
→ Очистите browser cache (Ctrl+Shift+Delete)
```

### Проблема: "ERR_CONNECTION_REFUSED"
```
→ Убедитесь что npm run dev запущен
→ Откройте http://localhost:5173 (а не http://localhost:3000)
```

### Проблема: "401 Unauthorized"
```
→ Значит старые ключи - нужны НОВЫЕ из Back4App
→ Выполните ШАГ 1 и ШАГ 2
```

### Проблема: "Не могу создать товар"
```
→ Это нормально до настройки Cloud Code (ШАГ 6)
→ Сначала надо настроить ACL (ШАГ 5)
```

---

## ✅ КОГДА ВСЕ ГОТОВО

Вы узнаете что все сделано когда:

```
✅ npm run dev работает без ошибок
✅ Форма входа отображается в браузере
✅ Можно создать товар
✅ DevTools → Network: нет REST_KEY
✅ Back4App → Logs: видны операции
✅ Production готов к запуску
```

---

**Начните с ШАГ 1: Регенерируйте ключи в Back4App!** 🚀

---

*Полный список действий*  
*Версия: 1.0*  
*Дата: 1 декабря 2025 г.*
