# ⚠️ ВАЖНО: НЕ ДОБАВЛЯЙТЕ РЕАЛЬНЫЕ ЗНАЧЕНИЯ В ЭТОТ ФАЙЛ!
# Это только пример структуры для .env.local

# Back4App / Parse SDK Configuration
# Получите эти значения из Back4App Settings → Keys

# ✓ БЕЗОПАСНО (используется на клиенте):
VITE_PARSE_APP_ID=your_app_id_here
VITE_PARSE_JS_KEY=your_js_key_here

# ❌ ОПАСНО для клиента (используйте ТОЛЬКО на сервере):
# VITE_PARSE_REST_KEY=your_rest_key_here
# VITE_PARSE_MASTER_KEY=your_master_key_here

# Другие переменные (по необходимости):
# VITE_API_URL=http://localhost:3000
# VITE_GOOGLE_GEMINI_KEY=your_key_here

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# ИНСТРУКЦИЯ ПО ИСПОЛЬЗОВАНИЮ:
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# 1. Скопируйте этот файл в .env.local:
#    cp .env.example .env.local

# 2. Откройте Back4App (https://www.back4app.com)

# 3. В Settings → Keys скопируйте:
#    - Application ID → VITE_PARSE_APP_ID
#    - JavaScript Key → VITE_PARSE_JS_KEY

# 4. НИКОГДА не добавляйте REST_KEY в .env.local!
#    REST_KEY используется ТОЛЬКО на сервере (Cloud Code)

# 5. Проверьте .gitignore содержит *.local
#    (чтобы .env.local не был загружен в Git)

# 6. Запустите приложение:
#    npm install
#    npm run dev

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# БЕЗОПАСНОСТЬ:
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# 🔴 ОПАСНО:
# ❌ Не коммитьте .env.local в Git
# ❌ Не используйте REST_KEY на клиенте
# ❌ Не добавляйте keys в документацию
# ❌ Не делитесь ключами в интернете

# 🟢 ПРАВИЛЬНО:
# ✓ Используйте .env.example для примера
# ✓ Используйте .env.local только локально
# ✓ Регулярно ротируйте ключи
# ✓ Используйте разные ключи для dev/prod
# ✓ Храните REST_KEY на сервере
# ✓ Используйте Cloud Code для операций

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# ДОПОЛНИТЕЛЬНО:
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# Для production используйте переменные окружения хостера:
# - Vercel: Project Settings → Environment Variables
# - Netlify: Site Settings → Build & Deploy → Environment
# - GitHub Pages: Settings → Secrets and variables → Actions
# - VPS: Отредактируйте .env.local на сервере вручную

# Ссылки:
# - Back4App Docs: https://www.back4app.com/docs
# - Vite Env Docs: https://vitejs.dev/guide/env-and-mode.html
