# 🏗️ АРХИТЕКТУРА БЕЗОПАСНОСТИ - ВИЗУАЛЬНОЕ РУКОВОДСТВО

**Версия**: 1.0  
**Дата**: 1 декабря 2025 г.  
**Статус**: ✅ Production-Ready Architecture

---

## 🎯 ЦЕЛЕВАЯ АРХИТЕКТУРА БЕЗОПАСНОСТИ

### БЫЛО (❌ ОПАСНО):

```
┌─────────────────────────────────────────┐
│         БРАУЗЕР ПОЛЬЗОВАТЕЛЯ            │
│                                         │
│  ┌─────────────────────────────────┐   │
│  │  REACT ПРИЛОЖЕНИЕ               │   │
│  │                                 │   │
│  │  APP_ID      ✓                 │   │
│  │  JS_KEY      ✓                 │   │
│  │  REST_KEY    ❌ ОПАСНО!        │   │  ← ПОЛНЫЙ ДОСТУП К БД!
│  │  MASTER_KEY  ❌ ОПАСНО!        │   │
│  │                                 │   │
│  │  ┌─────────────────────────┐   │   │
│  │  │ Может:                  │   │   │
│  │  │ ✗ Читать все данные     │   │   │
│  │  │ ✗ Создавать что угодно  │   │   │
│  │  │ ✗ Удалять все          │   │   │
│  │  │ ✗ Менять другим юзерам │   │   │
│  │  └─────────────────────────┘   │   │
│  └─────────────────────────────────┘   │
│                                         │
│  localStorage:                          │
│  - sessionToken ✓                       │
│  - userId ✓                             │
│  - REST_KEY ❌ ВИДНО В DEVTOOLS         │
│                                         │
└─────────────────────────────────────────┘
         │ HTTP Запрос
         │ (REST_KEY в заголовке)
         ▼
┌─────────────────────────────────────────┐
│      BACK4APP / PARSE SERVER            │
│                                         │
│  Выполняет ВСЕ запросы без проверок!   │
│  Думает что это admin (ведь REST_KEY)   │
│                                         │
│  ✗ Нет защиты от злоумышленников        │
│  ✗ Нет логирования кто что делает       │
│  ✗ Нет ограничений по операциям         │
│                                         │
└─────────────────────────────────────────┘
```

**Проблемы:**
- 🔴 REST_KEY видна в браузере (в DevTools, сетевом трафике)
- 🔴 Злоумышленник может получить доступ к ЛЮБЫМ данным
- 🔴 Можно удалить все товары, заказы, пользователей
- 🔴 Можно создать поддельные заказы с деньгами
- 🔴 Можно изменить пароли других пользователей

---

### БУДЕТ (✅ БЕЗОПАСНО):

```
┌─────────────────────────────────────────────────────┐
│           БРАУЗЕР ПОЛЬЗОВАТЕЛЯ (Web Browser)         │
│                                                     │
│  ┌───────────────────────────────────────────────┐  │
│  │  REACT ПРИЛОЖЕНИЕ                            │  │
│  │                                               │  │
│  │  ✓ APP_ID (Application ID)                   │  │
│  │  ✓ JS_KEY (JavaScript Key)                   │  │
│  │  ✓ Session Token (после входа)               │  │
│  │  ✗ REST_KEY (НЕ отправляется!)              │  │
│  │  ✗ MASTER_KEY (НЕ отправляется!)            │  │
│  │                                               │  │
│  │  ┌──────────────────────────────────────┐    │  │
│  │  │ Может (только свои данные):          │    │  │
│  │  │ ✓ Читать PUBLIC данные              │    │  │
│  │  │ ✓ Читать ЕГО товары/заказы         │    │  │
│  │  │ ✓ Создавать свои товары             │    │  │
│  │  │ ✓ Менять только ЕГО профиль        │    │  │
│  │  │ ✗ Читать чужие данные               │    │  │
│  │  │ ✗ Удалять чужие товары              │    │  │
│  │  │ ✗ Менять другим юзерам              │    │  │
│  │  └──────────────────────────────────────┘    │  │
│  │                                               │  │
│  │  localStorage:                                │  │
│  │  ✓ sessionToken (временный, зашифрован)     │  │
│  │  ✓ userId                                    │  │
│  │  ✗ REST_KEY (НЕ хранится!)                  │  │
│  │  ✗ MASTER_KEY (НЕ хранится!)                │  │
│  └───────────────────────────────────────────────┘  │
│                                                     │
│  DevTools (Ctrl+Shift+I):                          │
│  ✓ Видны только нормальные запросы                │
│  ✗ REST_KEY НЕ видна                             │
│  ✗ MASTER_KEY НЕ видна                           │
│                                                     │
└─────────────────────────────────────────────────────┘
         │ HTTP Запрос
         │ (Session Token, User ID)
         │ NO REST_KEY!
         ▼
┌─────────────────────────────────────────────────────┐
│    NODEJS BACKEND / CLOUD CODE (Ваш сервер)        │
│                                                     │
│  ✓ APP_ID                                           │
│  ✓ JS_KEY                                           │
│  ✓ REST_KEY (в переменной окружения, скрыто)       │
│  ✓ MASTER_KEY (опционально)                         │
│  ✓ Session Token (из браузера)                      │
│  ✓ User ID (из браузера)                            │
│                                                     │
│  ┌─────────────────────────────────────────────┐   │
│  │ Функции (Parse.Cloud.define):               │   │
│  │ - updateProductImage()                      │   │
│  │ - createOrder()                             │   │
│  │ - processPayment()                          │   │
│  │ - etc.                                      │   │
│  └─────────────────────────────────────────────┘   │
│                                                     │
│  ЛОГИРОВАНИЕ:                                       │
│  ✓ Кто что делает                                  │
│  ✓ Когда было сделано                              │
│  ✓ Успешно/ошибка                                  │
│  ✓ IP адрес                                        │
│                                                     │
│  ВАЛИДАЦИЯ:                                         │
│  ✓ Проверка прав доступа (ACL)                     │
│  ✓ Проверка данных (schema)                        │
│  ✓ Проверка бизнес-логики                          │
│                                                     │
│  ┌─────────────────────────────────────────────┐   │
│  │ Может:                                      │   │
│  │ ✓ Выполнять admin операции                 │   │
│  │ ✓ Проверять права на операции              │   │
│  │ ✓ Выполнять сложные вычисления             │   │
│  │ ✓ Логировать операции                      │   │
│  │ ✓ Вызывать внешние API                     │   │
│  └─────────────────────────────────────────────┘   │
│                                                     │
└─────────────────────────────────────────────────────┘
         │ REST API
         │ (REST_KEY в заголовке Authorization)
         │ (Request подписана на сервере)
         ▼
┌─────────────────────────────────────────────────────┐
│    BACK4APP / PARSE SERVER (Cloud Database)         │
│                                                     │
│  Проверяет ЧТО и КТО запрашивает:                  │
│                                                     │
│  REQUEST: {                                         │
│    "method": "GET",                                 │
│    "path": "/parse/classes/Product",               │
│    "sessionToken": "xyz123",                        │
│    "userId": "user_123"                             │
│  }                                                  │
│                                                     │
│  ┌─────────────────────────────────────────────┐   │
│  │ ПРОВЕРКА:                                   │   │
│  │ 1. sessionToken валиден?                    │   │
│  │    → Да ✓ продолжить                        │   │
│  │ 2. userId совпадает с tokenом?              │   │
│  │    → Да ✓ продолжить                        │   │
│  │ 3. ACL правила позволяют?                   │   │
│  │    → Да ✓ вернуть данные                    │   │
│  │    → Нет ✗ вернуть 403 Forbidden            │   │
│  │ 4. Есть ограничения по rate?                │   │
│  │    → Да ✓ применить                         │   │
│  └─────────────────────────────────────────────┘   │
│                                                     │
│  ✓ Каждый запрос логируется                        │
│  ✓ Попытки взлома видны                            │
│  ✓ Можно заблокировать IP                          │
│                                                     │
│  DATABASE:                                          │
│  - Products                                         │
│  - Orders                                           │
│  - Transactions                                     │
│  - Users                                            │
│  - Files (images)                                   │
│                                                     │
└─────────────────────────────────────────────────────┘
```

**Улучшения:**
- ✅ REST_KEY только на сервере (скрыто в переменных окружения)
- ✅ Браузер может только читать PUBLIC данные
- ✅ Все операции логируются (аудит)
- ✅ ACL защищает доступ к конкретным объектам
- ✅ Cloud Code валидирует все операции
- ✅ SessionToken временный и ограниченный

---

## 🔐 СЛОИ БЕЗОПАСНОСТИ

```
УРОВЕНЬ 1: БРАУЗЕР
┌─────────────────────────────────────────┐
│ ✓ Валидация формы                       │
│ ✓ HTTPS (зашифрованный трафик)          │
│ ✓ Content Security Policy (CSP)         │
│ ✓ Sanitize HTML (защита от XSS)         │
│ ✓ SessionToken в хранилище (не cookie) │
└─────────────────────────────────────────┘
           ↓

УРОВЕНЬ 2: СЕТЕВОЙ ТРАНСПОРТ
┌─────────────────────────────────────────┐
│ ✓ HTTPS (TLS 1.2+)                      │
│ ✓ CORS (только разрешенные домены)      │
│ ✓ HSTS (переживите с HTTP на HTTPS)     │
│ ✓ Signature verification                │
└─────────────────────────────────────────┘
           ↓

УРОВЕНЬ 3: BACKEND / CLOUD CODE
┌─────────────────────────────────────────┐
│ ✓ Request validation                    │
│ ✓ Authentication (sessionToken)         │
│ ✓ Authorization (ACL)                   │
│ ✓ Rate limiting                         │
│ ✓ Input sanitization                    │
│ ✓ Logging & monitoring                  │
│ ✓ Error handling                        │
└─────────────────────────────────────────┘
           ↓

УРОВЕНЬ 4: DATABASE
┌─────────────────────────────────────────┐
│ ✓ ACL правила (Object-level)            │
│ ✓ Column restrictions                   │
│ ✓ Encryption at rest                    │
│ ✓ Backups & disaster recovery           │
│ ✓ Audit logging                         │
│ ✓ DDoS protection                       │
└─────────────────────────────────────────┘
```

---

## 📋 ПРИМЕРЫ ПОТОКОВ ДАННЫХ

### ✅ ПРАВИЛЬНЫЙ ПОТОК (Вендор получает свои товары)

```
1. БРАУЗЕР
   Пользователь кликает "Мои товары"
   │
   ├─ Проверка: есть sessionToken? ✓
   ├─ Проверка: sessionToken валиден? ✓
   │
   └─ Parse.Query('Product')
      .equalTo('vendorId', currentUser.id)
      .find()

2. СЕТЕВОЙ ЗАПРОС
   GET /parse/classes/Product?where={"vendorId":"user_123"}
   Headers: {
     "X-Parse-Session-Token": "xyz...",
     "Content-Type": "application/json"
   }
   ✓ REST_KEY НЕ ОТПРАВЛЯЕТСЯ!

3. BACKEND / PARSE SERVER
   Проверка:
   ├─ sessionToken = "xyz..." в БД? ✓
   ├─ userId из token = "user_123"? ✓
   ├─ ACL разрешает read для этого юзера? ✓
   └─ Вернуть только товары где vendorId = user_123

4. БАЗА ДАННЫХ
   SELECT * FROM Product WHERE vendorId = 'user_123'
   ├─ Проверка ACL: только владелец может читать ✓
   └─ Вернуть объекты

5. РЕЗУЛЬТАТ
   ✅ 200 OK - товары владельца
   ✅ Логировано: who, what, when
```

---

### ❌ НЕПРАВИЛЬНЫЙ ПОТОК (Хакер пытается удалить товары)

```
1. БРАУЗЕР (хакера)
   Открывает DevTools → Console
   Пытается:
   
   const query = new Parse.Query('Product');
   query.limit(10000);
   query.destroyAll(); // Удалить все!
   ❌ Без sessionToken!
   ❌ С REST_KEY из браузера!

2. СЕТЕВОЙ ЗАПРОС
   DELETE /parse/classes/Product
   Headers: {
     "X-Parse-REST-API-Key": "BadiMhfYEd68..." ❌ ВИДНА!
   }

3. BACKEND (БЕЗ нашего исправления)
   ❌ Видит REST_KEY → это admin!
   ❌ Нет sessionToken → ignore ACL!
   ❌ Удаляет ВСЕ товары!
   ❌ Лог: "unknown user deleted products"

4. РЕЗУЛЬТАТ
   🔴 КАТАСТРОФА! Все товары удалены!
   🔴 Функция поиска не работает!
   🔴 Пользователи видят ошибки!
```

---

### ✅ ПРАВИЛЬНЫЙ ПОТОК (С нашим исправлением)

```
1. БРАУЗЕР (хакера)
   Пытается удалить товары
   ❌ В браузере НЕТ REST_KEY (мы удалили!)
   ❌ Может отправить только с sessionToken

2. СЕТЕВОЙ ЗАПРОС
   DELETE /parse/classes/Product
   Headers: {
     "X-Parse-Session-Token": "hacker_token"
   }
   ✓ REST_KEY не отправляется!

3. BACKEND / CLOUD CODE
   Проверка:
   ├─ sessionToken = "hacker_token"? Да ✓
   ├─ Это какой пользователь? user_hack ✓
   ├─ ACL: может ли user_hack удалять Product? 
   │        → Нет, может только удалять СВОИ товары ✗
   └─ Вернуть 403 Forbidden

4. БАЗА ДАННЫХ
   ❌ Запрос даже не доходит!

5. РЕЗУЛЬТАТ
   ✅ 403 Forbidden - доступ запрещен
   ✅ Логировано: user_hack tried unauthorized delete
   ✅ Можно заблокировать этого пользователя
```

---

## 🛡️ ACL ПРАВИЛА ПО КЛАССАМ

### Product (Товары)

```
┌──────────────────────────────────────┐
│ Object: Product {                    │
│   "id": "prod_123",                  │
│   "name": "iPhone 15",               │
│   "vendorId": "vendor_42",           │
│   "price": 1200,                     │
│   ...                                │
│ }                                    │
│                                      │
│ ACL: {                               │
│   "public": {                        │
│     "read": true,  // Все видят ✓   │
│     "write": false // Не могут менять│
│   },                                 │
│   "vendor_42": {                     │
│     "read": true,  // Может читать   │
│     "write": true  // Может менять ✓│
│   },                                 │
│   "vendor_99": {                     │
│     "read": false, // Не видит ✗    │
│     "write": false // Не может менять│
│   }                                  │
│ }                                    │
└──────────────────────────────────────┘

Результат:
- PUBLIC (все): читают только ✓
- OWNER (vendor_42): читают + пишут ✓
- OTHER VENDORS: не видят ✗
```

### Order (Заказы)

```
┌──────────────────────────────────────┐
│ Object: Order {                      │
│   "id": "order_789",                 │
│   "customerId": "customer_1",        │
│   "vendorId": "vendor_42",           │
│   "items": [...],                    │
│   "total": 2500,                     │
│   ...                                │
│ }                                    │
│                                      │
│ ACL: {                               │
│   "public": {                        │
│     "read": false, "write": false    │
│   },                                 │
│   "customer_1": {                    │
│     "read": true,  // Видит свой    │
│     "write": false // Не может менять│
│   },                                 │
│   "vendor_42": {                     │
│     "read": true,  // Видит заказ   │
│     "write": true  // Может обновить статус ✓│
│   }                                  │
│ }                                    │
└──────────────────────────────────────┘

Результат:
- PUBLIC: скрыто ✓
- CUSTOMER: читает только ✓
- VENDOR: читает + обновляет статус ✓
```

### User (Пользователи)

```
┌──────────────────────────────────────┐
│ Object: User {                       │
│   "id": "user_123",                  │
│   "username": "john_doe",            │
│   "email": "john@example.com",       │
│   "password": "bcrypt_hash",         │
│   "role": "vendor",                  │
│   "phone": "+1234567890",            │
│   ...                                │
│ }                                    │
│                                      │
│ ACL: {                               │
│   "public": {                        │
│     "read": false, "write": false    │
│   },                                 │
│   "user_123": {                      │
│     "read": true,  // Видит свой    │
│     "write": true  // Может менять ✓│
│   },                                 │
│   "user_999": {                      │
│     "read": false, // Не видит ✗    │
│     "write": false // Не может менять│
│   }                                  │
│ }                                    │
└──────────────────────────────────────┘

Результат:
- PUBLIC: скрыто ✓
- OWNER: полный доступ ✓
- OTHER USERS: полностью скрыто ✗
```

---

## 🚀 ПРИМЕРЫ CLOUD CODE

### Пример 1: Безопасное обновление товара

```javascript
// Back4App Cloud Code
// ✓ REST_KEY используется только здесь!

Parse.Cloud.define('updateProduct', async (request) => {
  const { productId, name, price, description } = request.params;
  const user = request.user;
  
  // 1. ПРОВЕРКА: Пользователь авторизован?
  if (!user) {
    throw new Parse.Error(401, 'Требуется авторизация');
  }
  
  // 2. ПРОВЕРКА: Пользователь вендор?
  if (user.get('role') !== 'vendor') {
    throw new Parse.Error(403, 'Только вендоры могут обновлять товары');
  }
  
  // 3. ПОЛУЧИТЬ товар
  const Product = Parse.Object.extend('Product');
  const query = new Parse.Query(Product);
  const product = await query.get(productId);
  
  if (!product) {
    throw new Parse.Error(404, 'Товар не найден');
  }
  
  // 4. ПРОВЕРКА: Это товар этого вендора?
  if (product.get('vendorId') !== user.id) {
    throw new Parse.Error(403, 'Вы не владелец этого товара');
  }
  
  // 5. ВАЛИДАЦИЯ данных
  if (!name || name.length < 3) {
    throw new Parse.Error(400, 'Название должно быть минимум 3 символа');
  }
  if (price < 0) {
    throw new Parse.Error(400, 'Цена не может быть отрицательной');
  }
  
  // 6. ОБНОВЛЕНИЕ
  product.set('name', name);
  product.set('price', price);
  product.set('description', description);
  product.set('updatedAt', new Date());
  
  // 7. ЛОГИРОВАНИЕ
  console.log(`[AUDIT] User ${user.id} updated product ${productId}`);
  
  // 8. СОХРАНЕНИЕ (используем REST_KEY на сервере!)
  return product.save();
});
```

**Использование на клиенте:**

```typescript
// ✓ БРАУЗЕР - безопасно!
const result = await Parse.Cloud.run('updateProduct', {
  productId: 'prod_123',
  name: 'iPhone 15 Pro',
  price: 1299,
  description: 'Latest model'
});

// ✗ НЕ REST_KEY!
// ✓ Только sessionToken!
```

---

### Пример 2: Безопасное создание заказа

```javascript
// Back4App Cloud Code

Parse.Cloud.define('createOrder', async (request) => {
  const { items, address } = request.params;
  const user = request.user;
  
  if (!user) {
    throw new Parse.Error(401, 'Требуется авторизация');
  }
  
  // Проверка items (не может быть пустым)
  if (!items || items.length === 0) {
    throw new Parse.Error(400, 'Заказ должен содержать товары');
  }
  
  // Валидация каждого товара
  let total = 0;
  for (const item of items) {
    const Product = Parse.Object.extend('Product');
    const query = new Parse.Query(Product);
    const product = await query.get(item.productId);
    
    if (!product) {
      throw new Parse.Error(404, `Товар ${item.productId} не найден`);
    }
    
    // Проверка цены (не дать изменить цену на лету!)
    if (product.get('price') !== item.price) {
      throw new Parse.Error(400, 'Цена товара изменилась, перезагрузите страницу');
    }
    
    total += item.price * item.quantity;
  }
  
  // Создание заказа
  const Order = Parse.Object.extend('Order');
  const order = new Order();
  
  order.set('customerId', user.id);
  order.set('items', items);
  order.set('address', address);
  order.set('total', total);
  order.set('status', 'pending');
  order.set('createdAt', new Date());
  
  // Установка ACL (только владелец может читать)
  const acl = new Parse.ACL(user);
  acl.setPublicReadAccess(false);
  order.setACL(acl);
  
  // Логирование
  console.log(`[AUDIT] User ${user.id} created order for ${total}`);
  
  return order.save();
});
```

---

## 📊 МАТРИЦА ДОСТУПА

```
                    | PUBLIC | CUSTOMER | VENDOR | ADMIN |
                    |--------|----------|--------|-------|
Product             |        |          |        |       |
├─ Read             |  ✓✓✓  |   ✓✓✓   |  ✓✓✓ |  ✓✓✓ |
├─ Create           |  ✗✗✗  |   ✗✗✗   |  ✓✓✓ |  ✓✓✓ |
├─ Update (owner)   |  ✗✗✗  |   ✗✗✗   |  ✓✓✓ |  ✓✓✓ |
├─ Delete (owner)   |  ✗✗✗  |   ✗✗✗   |  ✓✓✓ |  ✓✓✓ |
└─ Delete (other)   |  ✗✗✗  |   ✗✗✗   |  ✗✗✗ |  ✓✓✓ |
                    |        |          |        |       |
Order               |        |          |        |       |
├─ Read             |  ✗✗✗  |  ✓✓✓ |  ✓✓✓ |  ✓✓✓ |
├─ Create           |  ✗✗✗  |  ✓✓✓ |  ✗✗✗ |  ✓✓✓ |
├─ Update (owner)   |  ✗✗✗  |  ✓✓✓ |  ✓✓✓ |  ✓✓✓ |
├─ Delete (owner)   |  ✗✗✗  |  ✗✗✗  |  ✗✗✗ |  ✓✓✓ |
└─ Change status    |  ✗✗✗  |  ✗✗✗  |  ✓✓✓ |  ✓✓✓ |
                    |        |          |        |       |
User                |        |          |        |       |
├─ Read             |  ✗✗✗  |  ✓(себя) |  ✓(себя) |  ✓✓✓ |
├─ Update           |  ✗✗✗  |  ✓(себя) |  ✓(себя) |  ✓✓✓ |
└─ Delete           |  ✗✗✗  |  ✗✗✗  |  ✗✗✗ |  ✓✓✓ |

Легенда:
✓✓✓ = Полный доступ (читать, писать, удалять)
✓✓  = Ограниченный доступ (читать, писать)
✓   = Минимальный доступ (только читать)
✗   = Доступ запрещен
```

---

## 🎓 ИТОГИ

### Клиент (Браузер) получает:
- ✓ APP_ID (знают все)
- ✓ JS_KEY (для авторизации)
- ✓ Session Token (временный, после входа)

### Клиент НЕ получает:
- ✗ REST_KEY (полный доступ)
- ✗ MASTER_KEY (админ доступ)

### Сервер (Cloud Code) получает:
- ✓ Все ключи (в переменных окружения)
- ✓ Session Token от браузера
- ✓ Полную информацию о пользователе

### Защита в БД:
- ✓ ACL правила (кто может что делать)
- ✓ Cloud Code функции (проверка логики)
- ✓ Rate limiting (от DDoS)
- ✓ Логирование (аудит)

---

**Результат: PRODUCTION-READY SECURITY! 🔒**

*Документ создан: 1 декабря 2025 г.*
